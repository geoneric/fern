import unittest
import fern.feature as ff


class MaskedConstantTest(unittest.TestCase):

    def test_construction(self):
        pass
        # constant = ff.masked_constant(3, False)
        # self.assertEqual(type(constant), ff.MaskedConstant)
        # self.assertEqual(constant.value, 3)
        # self.assertEqual(constant.masked, False)

        # constant = ff.masked_constant(4, True)
        # self.assertEqual(type(constant), ff.MaskedConstant)
        # self.assertEqual(constant.value, 4)
        # self.assertEqual(constant.masked, True)
