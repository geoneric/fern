add_subdirectory(neighborhood)
add_test_conditionally(test)

set(SOURCES
    # index_ranges
)
# add_object_library(
#     TARGET fern_algorithm_convolution_objects
#     SOURCES ${SOURCES}
#     LIBRARY fern_algorithm)
