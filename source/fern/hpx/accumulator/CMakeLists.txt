include_directories(
    ${CMAKE_CURRENT_SOURCE_DIR})
add_executable(managed_accumulator_client
    managed_accumulator
    managed_accumulator_client
)
target_link_libraries(managed_accumulator_client
    hpx_init
    hpx
    ${Boost_serialization_LIBRARY}
)
