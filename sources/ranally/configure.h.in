#pragma once

#define RANALLY_BUILD_TYPE "${CMAKE_BUILD_TYPE}"
#define RANALLY_SYSTEM "${CMAKE_SYSTEM}"
#define RANALLY_CXX_COMPILER "${CMAKE_CXX_COMPILER}"

#define RANALLY_MAJOR_VERSION ${${CMAKE_PROJECT}_MAJOR_VERSION}
#define RANALLY_MINOR_VERSION ${${CMAKE_PROJECT}_MINOR_VERSION}
#define RANALLY_PATCH_VERSION ${${CMAKE_PROJECT}_PATCH_VERSION}
// #define RANALLY_PRERELEASE_VERSION "${${CMAKE_PROJECT}_PRE_RELEASE_VERSION}"
#define RANALLY_VERSION "${${CMAKE_PROJECT}_VERSION}"
#define RANALLY_COPYRIGHT "${${CMAKE_PROJECT}_COPYRIGHT}"

#define RANALLY_ARCHITECTURE "${ARCHITECTURE}"

namespace ranally {

} // namespace ranally
