INCLUDE(UseBoost)
INCLUDE(UseIcu)

INCLUDE_DIRECTORIES(
    ${CMAKE_CURRENT_SOURCE_DIR}/../..
)

ADD_SUBDIRECTORY(Tests)

SET(SOURCES
    String.cc
)
ADD_LIBRARY(RanallyUtil
    ${SOURCES}
)
TARGET_LINK_LIBRARIES(RanallyUtil
    ${ICU_LIBRARIES}
)
