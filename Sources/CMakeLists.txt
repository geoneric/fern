CONFIGURE_FILE(
    ${CMAKE_CURRENT_SOURCE_DIR}/Configure.h.in
    ${CMAKE_CURRENT_BINARY_DIR}/Ranally/Configure.h
)

IF(DOXYGEN_FOUND)
    INCLUDE(DoxygenDoc)
ENDIF()

INCLUDE_DIRECTORIES(
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_CURRENT_BINARY_DIR}
)

ADD_SUBDIRECTORY(Ranally)
